/**
 * Shared Recharts motion — aligns with Insights overview pie (sequential sector sweep)
 * and keeps bar/area/line charts feeling consistent across the app.
 */
export const AMIIO_CHART_MOTION = {
  isAnimationActive: true as const,
  animationDuration: 1000,
  animationEasing: "ease-out" as const,
};
